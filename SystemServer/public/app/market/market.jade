table.ui.celled.structured.table(ng-controller="MarketController")
  thead
    tr
      th Auction Number
      th Bidder
      th Energy Bid
      th Price Bid
      th Block Start
      th Block Duration
  tbody
    tr(ng-repeat="data in tableRows | reverse")
      //- td(ng-if="data[$index][rowspan]" rowspan="{{data[$index][rowspan]}}") {{$index + 1}}
      td {{data.auctionNum}}
      td {{data.bidder}}
      td {{data.energyBid}}
      td {{data.priceBid}}
      td {{data.blockStart}}
      td {{data.blockDuration}}

    //- tr(ng-repeat="data in aggregations.auctions.auctions.array")
    //-   td(rowspan="{{objectLength(data.bidders)}}") {{$index+1}}
    //-   td {{objectKeys(data.bidders)[0]}}
    //-   td {{data.bids[0].energy}}
    //-   td {{data.bids[0].price}}
    //-   td {{data.currentBlock.blockStart}}
    //-   td {{data.currentBlock.blockDuration}}
    //- tr(ng-repeat="(bidder,bidderdata) in data.bidders")
    //-   td {{objectKeys(data.bidders)[$index + 1]}}
    //-   td {{data.bids[$index + 1].energy}}
    //-   td {{data.bids[$index + 1].price}}
    //-   td {{data.currentBlock.blockStart}}
    //-   td {{data.currentBlock.blockStart}}
